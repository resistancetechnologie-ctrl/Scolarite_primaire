name: ecole_primaire
description: Gestion scolaire complete pour ecole primaire - 100% hors ligne (offline).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  path_provider: ^2.1.4
  intl: ^0.19.0
  uuid: ^4.4.0
  crypto: ^3.0.3
  file_picker: ^11.0.2
  image_picker: ^1.2.3
  share_plus: ^13.2.0
  pdf: ^3.11.1
  printing: ^5.15.0
  qr_flutter: ^4.1.0
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
